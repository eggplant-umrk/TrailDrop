# infra

TrailDrop のインフラ関連ファイルを置くディレクトリです。

- `schema.sql` — Supabase (PostgreSQL) の初期スキーマ定義。Supabase SQL Editor またはCLI (`supabase db push`) で適用します。
- デプロイ設定ファイル（Vercel / Docker / GitHub Actions など）は今後このディレクトリに追加予定です。
